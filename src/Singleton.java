import com.sun.xml.internal.ws.wsdl.writer.UsingAddressing;

public class Singleton {
    private volatile Singleton singleton;

    public synchronized void Singleton(){
        if (singleton==null)
        {

        }
    }
}
